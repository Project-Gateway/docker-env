version: '3'
services:

  web:
    build:
      context: services/web
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./api:/var/www/login-api
      - ./client/dist:/var/www/client
      - ./services/web/conf/nginx.conf:/etc/nginx/conf.d/default.conf
      - ./services/web/conf/snippets/self-signed.conf:/etc/nginx/snippets/self-signed.conf
      - ./services/web/conf/snippets/ssl-params.conf:/etc/nginx/snippets/ssl-params.conf
      - ./services/web/ssl/certs/dhparam.pem:/etc/ssl/certs/dhparam.pem
      - ./services/ssl/server.crt:/etc/ssl/certs/server.crt
      - ./services/ssl/server.key:/etc/ssl/private/server.key
    depends_on:
      - login-api
      - login-ui

  login-ui:
      build:
        context: services/client
      working_dir: /client
      environment:
        - NODE_ENV=development
      volumes:
        - ../login-ui-vue:/client
        - ./docker/ssl/server.crt:/etc/ssl/certs/server.crt
        - ./docker/ssl/server.key:/etc/ssl/private/server.key
      ports:
        - 8080:8080
      command: "npm run dev"

  login-api:
    build:
      context: services/api
    volumes:
      - ../login-api:/var/www/api
#    depends_on:
#      - postgres
#
#  realtime:
#    build:
#      context: docker/realtime
#    volumes:
#      - ./realtime:/realtime
#    ports:
#      - 3001:3001
#    working_dir: /realtime
#    environment:
#      - "SOCKET_PORT=3001"
#    entrypoint: [ "php", "./server.php" ]
#    links:
#      - postgres
#
#  composer:
#      build:
#        context: docker/composer
#      volumes:
#        - ./realtime:/realtime
#        - ./api:/api
#        - ./docker/composer/entrypoint.sh:/entrypoint/entrypoint.sh
#      working_dir: /entrypoint
#      entrypoint: [ "./entrypoint.sh" ]
#      #command: [ "install" ]
#
#  postgres:
#    build:
#      context: docker/postgres
#    command: postgres -c logging_collector=on -c log_destination=stderr -c log_directory=/var/log/postgres -c config_file=/etc/postgres/postgresql.conf
#    volumes:
#      - ./docker/postgres/data:/var/lib/postgresql/data
#      - ./docker/postgres/logs:/var/log/postgres
#      - ./docker/postgres/config:/etc/postgres
#    environment:
#      - "POSTGRES_PASSWORD=postgres"
